Per instal-lar els moduls necessaris:

$ npm install

Per engegar el servei de l'api:

$ sudo systemctl start api

Alternativament, tambe la podem engegar temporalment (no en background) amb:ç

$ npm start
